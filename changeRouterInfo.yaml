---
- name: Change Router Configuration
  hosts: CSR1kv1
  gather_facts: false
  connection: local

  tasks:
    - name: Change hostname
      ios_command:
        commands:
          - configure terminal
          - hostname CSRouter45
          - exit
